import * as types from './types';

export function abc(text){
    return {
        type:types.abc,
        text
    }
};
export function bcd(text) {
    return {
        type: types.abc,
        text
    }
}